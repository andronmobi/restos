<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andronblog.restos" >

    <uses-permission android:name="com.andronblog.restos.provider.READ_DATABASE"/>
    <uses-permission android:name="com.andronblog.restos.provider.WRITE_DATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CityInsertActivity"
            android:allowEmbedded="@string/app_name"/>

        <activity
            android:name=".CityUpdateActivity"
            android:allowEmbedded="@string/app_name"/>

    </application>

</manifest>
